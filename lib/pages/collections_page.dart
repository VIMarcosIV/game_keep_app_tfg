import '../library/imports.dart';

class Collections_Page extends StatelessWidget {
  const Collections_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
